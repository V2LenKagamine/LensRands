using LensRands.Systems.ModSys;
using Terraria;
using Terraria.ModLoader;
using Terraria.UI;

namespace LensRands
{
	public class LensRands : Mod
	{
        public const string AssetsPath = $"{nameof(LensRands)}/Assets/";
        /*
        public override void Load()
        {
        }
        public override void Unload()
        {
        }
        */
    }
}